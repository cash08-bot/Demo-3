name: CDK Deploy (Auto after PR merge)

on:
  push:
    branches:
      - dev
      - stg

permissions:
  id-token: write
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        include:
          - branch: dev
            env_name: dev
            role_arn: arn:aws:iam::381492133980:role/GitHubAction-AssumeRoleWithAction
          - branch: stg
            env_name: stg
            role_arn: arn:aws:iam::614056699201:role/Cdk-deploy-dev-demo

    environment: ${{ matrix.env_name }}

    env:
      ENV_ID: ${{ matrix.env_name }}
      AWS_REGION: us-east-1

    steps:
      - name: Print debug info
        run: |
          echo "Branch: ${{ matrix.branch }}"
          echo "Role ARN: ${{ matrix.role_arn }}"

      - name: Exit if not matching matrix branch
        if: ${{ github.ref != format('refs/heads/{0}', matrix.branch) }}
        run: echo "Branch mismatch. Skipping..." && exit 0

      - name: Checkout source
        uses: actions/checkout@v4

      - name: Configure AWS Credentials (OIDC)
        uses: aws-actions/configure-aws-credentials@v3
        with:
          role-to-assume: ${{ matrix.role_arn }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Set up CDK and Maven
        run: |
          npm install -g aws-cdk
          mvn clean install

      - name: CDK Deploy
        run: |
          echo "Deploying to $ENV_ID"
          cdk deploy --require-approval never --context env=$ENV_ID
